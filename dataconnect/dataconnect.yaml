specVersion: "v1beta"
serviceId: "Webapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "Webapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
